interface:
  display_name: "Orbit: Create Todo"
  short_description: "Create a task in Orbit"
  default_prompt: "Create one Orbit task from my request."
policy:
  allow_implicit_invocation: false
dependencies:
  tools:
    - type: "mcp"
      value: "orbit"
      description: "Local Orbit task and ticket MCP server"
