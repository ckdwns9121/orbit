interface:
  display_name: "Orbit: Create Todos"
  short_description: "Create one or multiple Orbit tasks"
  default_prompt: "Use $orbit-create-todo to create these tasks in Orbit."
policy:
  allow_implicit_invocation: false
dependencies:
  tools:
    - type: "mcp"
      value: "orbit"
      description: "Local Orbit task and ticket MCP server"
